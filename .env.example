# Environment variables for Vercel, Netlify, Railway, or self-hosted Node.js.
# Generate a seed with `npm run keygen` and use the SAME value everywhere.
OHTTP_KEY_SEED=
TARGET_URL=https://target.ohttp.info
MAX_REQUEST_SIZE=1048576
CORS_ORIGIN=*
PORT=3000
